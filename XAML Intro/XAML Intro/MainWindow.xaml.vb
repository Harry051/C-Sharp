Class MainWindow
    '   Seem to have loaded VB not C# but "code behind" would look like this:

    'public MainWindow()
    '{
    '   IntiializeComponent();
    '   Grid grid = new Grid();
    '   this.Content = grid;
    '   Button btn = new Button();
    '   btn.Width = 100;
    '   btn.Height = 150;
    '   btn.FontSize = 28;

    '   WrapPanel wrapPanel = new WrapPanel();
    '   TextBlock txt = new TextBlock();
    '   txt.Text = "Multi";
    '   txt.Foreground = Brushes.Blue;
    '   wrapPanel.Children.Add(txt);

    '   WrapPanel wrapPanel = new WrapPanel();
    '   TextBlock txt = new TextBlock();
    '   txt.Text = "Colour";
    '   txt.Foreground = Brushes.White;
    '   wrapPanel.Children.Add(txt);

    '   WrapPanel wrapPanel = new WrapPanel();
    '   TextBlock txt = new TextBlock();
    '   txt.Text = "Button";
    '   txt.Foreground = Brushes.Red;
    '   wrapPanel.Children.Add(txt);

    '   btn.Content = wrapPanel;
    '   grid.Children.Add(btn);

    '

End Class
